if not millis then millis = "millis" end
if not parseBinaryRPCargument then parseBinaryRPCargument = "parseBinaryRPCargument" end
if not delay then delay = "delay" end
if not binaryRPCargumentByteLength then binaryRPCargumentByteLength = "binaryRPCargumentByteLength" end
if not serialiseBinaryRPCargument then serialiseBinaryRPCargument = "serialiseBinaryRPCargument" end
if not read_stdin then read_stdin = "read_stdin" end
if not memory64bitHash then memory64bitHash = "memory64bitHash" end
if not cmd_args then cmd_args = "cmd_args" end
if not unix_time then unix_time = "unix_time" end

